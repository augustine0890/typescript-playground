const isOpen: boolean = false;

const myName: string = "Augustine";

const myAge: number = 31;

const list: number[] = [1, 2, 3];

const me: [string, number] = ["Augustine", 31];
console.log(me);

enum Job { 
    SW, 
    Backend, 
    DE,
}

const job: Job = Job.SW;

const phone: any = "Anything";
const tablet: any = 1;

